# Copia este archivo como .env.sync (mismo folder) y pega la URL PUBLICA
# de la BD de Railway: proyecto -> servicio Postgres -> Variables ->
# DATABASE_PUBLIC_URL (la que contiene "proxy.rlwy.net").
PROD_DATABASE_URL=
